<h2>Latest posts</h2>

<% @results.each do |row| %>
	<p>
		<%= row['content']  %>
		<br><br>
		Added at <%= row['created_date'] %><br>
		<a href="/details/<%= row['id'] %>">Comments</a>
		<hr>
	</p>
<% end %>